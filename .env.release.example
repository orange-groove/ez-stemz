# Copy to `.env.release` and fill in. Sourced automatically by scripts/release.
# Never commit the real file.

# The exact identity from `security find-identity -v -p codesigning`.
DEVELOPER_ID_APPLICATION="Apple Development: Adam Groves (6SK2EKXQGA)"

# Apple ID + team for notarytool.
APPLE_ID="adam.groves333@gmail.com"
APPLE_TEAM_ID="XT486698Q5"

# App-specific password from https://appleid.apple.com → Sign-In and Security.
# (Not your real Apple ID password.)
APPLE_APP_PASSWORD="vkso-swci-bcyv-qlio"

# Optional: bundle a different model than the CMake default.
# EZSTEMZ_MODEL_FILE="/path/to/ggml-model-htdemucs-6s-f16.bin"
